<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for internet access -->
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SearchForCatImages"
        tools:targetApi="31">

        <meta-data
            android:name="com.github.bumptech.glide.REGISTERS_VERSION"
            android:value="1" />
        <meta-data
            android:name="com.github.bumptech.glide.integration.okhttp3.OkHttpUrlLoader.Factory"
            android:value="com.your.package.OkHttpUrlLoaderFactory" />
        <meta-data
            android:name="com.github.bumptech.glide.integration.recyclerview.RTL_LAYOUT_DIRECTION"
            android:value="rtl" />
        <meta-data
            android:name="com.github.bumptech.glide.integration.recyclerview.UseRegisterForActivityResults"
            android:value="true" />
        <service android:name=".util.CustomAppGlideModule" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <activity
            android:theme="@style/AppTheme"
            android:name=".ui.view.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
<!--            android:theme="@style/Theme.SearchForCatImages">-->
